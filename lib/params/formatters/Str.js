/*
 *   -= RestNio =-
 *  Made by 7kasper
 * Licensed under MIT
 *   -= RestNio =-
 */

class Str {

    static toLowerCase() {
        return (value) => {
            return value.toLowerCase();
        }
    }

    static toUpperCase() {
        return (value) => {
            return value.toUpperCase();
        }
    }

}
module.exports = Str;